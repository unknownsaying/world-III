#include "../R/rst123.c"
#include "../crak/ijk.c"
#include "math.h"
void t5t6(){
    char wx;
    double asin , acos;
    int t5, t6;
int ijk(){
    return i / asin(wx + t5) + j / acos(wx - t6);
    return k / asin(wx + t5) + l / acos(wx - t6);
    return i / asin(wx - t5) + j / acos(wx + t6);
    return k / asin(wx - t5) + l / acos(wx + t6);
}
}
