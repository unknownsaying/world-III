#include "../R/rst123.c"
#include "../crak/ijk.c"
#include "math.h"
void t5t6(){
    char wx;
    int t5, t6;
int ijk(){
    return I / asin(wx + t5) + J / acos(wx - t6) + K / asin(wx + t5) + L / acos(wx - t6);
    return i / asin(wx - t5) + j / acos(wx + t6) + k / asin(wx - t5) + l / acos(wx + t6);
}
}
