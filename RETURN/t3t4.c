#include "math.h"
void t3t4(){
    char xy;
    int t3, t4;
int efgh(){
    return e % exp(xy + t3) + f % log(xy - t4) + g % exp(xy + t3) + h % log(xy - t4);
    return E % exp(xy - t3) + F % log(xy + t4) + G % exp(xy - t3) + H  % log(xy + t4);
} 
}
