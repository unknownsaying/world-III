#include "../ABCD/A.c"
#include "../ABCD/B.c"
#include "../ABCD/C.c"
#include "../ABCD/D.c"
#include "math.h"
void t1t2(){
    char wx;
    double sin,cos;
    int t1 , t2;
int A B C D(){
    return A * sin(wx + t1) + B * cos(wx - t2) + C * sin(wx + t1) + D * cos(wx - t2);
    return A * sin(wx - t1) + B * cos(wx + t2) + C * sin(wx - t1) + D * cos(wx + t2);
} 
}
