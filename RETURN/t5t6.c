#include "math.h"
void t5t6(){
    char xz;
    int t5, t6;
int ijk(){
    return I / asin(xz + t5) + J / acos(xz - t6) + K / asin(xz + t5) + L / acos(xz - t6);
    return i / asin(xz - t5) + j / acos(xz + t6) + k / asin(xz - t5) + l / acos(xz + t6);
}
}
